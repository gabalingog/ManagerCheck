<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Manager Check — Light Concept</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --green:       #2a7a4b;
      --green-light: #3dab68;
      --green-pale:  #eaf5ee;
      --green-glow:  rgba(42,122,75,0.1);
      --cream:       #f5f0e8;
      --sand:        #ede6d8;
      --sand2:       #e4dccf;
      --warm-white:  #faf8f4;
      --surface:     #f0ebe1;
      --border:      rgba(60,45,20,0.1);
      --border2:     rgba(60,45,20,0.16);
      --text:        #1e1a14;
      --muted:       #7a6e5f;
      --muted2:      #a89d8e;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--cream);
      color: var(--text);
      font-family: 'DM Sans', sans-serif;
      font-weight: 300;
      overflow-x: hidden;
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 52px;
      height: 64px;
      background: rgba(245,240,232,0.88);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }

    .nav-logo {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 22px;
      letter-spacing: 0.12em;
      color: var(--text);
    }
    .nav-logo span { color: var(--green); }

    .nav-links { display: flex; align-items: center; gap: 8px; }

    .nav-login {
      background: none;
      border: none;
      color: var(--muted);
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      font-weight: 400;
      padding: 8px 20px;
      cursor: pointer;
      transition: color 0.2s;
    }
    .nav-login:hover { color: var(--text); }

    .nav-signup {
      background: var(--green);
      border: none;
      color: #fff;
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      font-weight: 500;
      padding: 9px 22px;
      border-radius: 4px;
      cursor: pointer;
      transition: background 0.2s, transform 0.15s;
    }
    .nav-signup:hover { background: var(--green-light); transform: translateY(-1px); }

    /* HERO */
    .hero {
      position: relative;
      height: 100vh;
      min-height: 700px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      background-image: // insert image here
      background-size: cover;
      background-position: center 45%;
      filter: brightness(0.55) saturate(0.8);
      transform: scale(1.05);
      animation: slowZoom 18s ease-out forwards;
    }

    @keyframes slowZoom {
      from { transform: scale(1.05); }
      to   { transform: scale(1.0); }
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background:
        radial-gradient(ellipse 65% 55% at 50% 115%, rgba(42,122,75,0.12) 0%, transparent 70%),
        linear-gradient(to bottom,
          rgba(30,20,10,0.25) 0%,
          rgba(30,20,10,0.08) 35%,
          rgba(245,240,232,0.0) 55%,
          rgba(245,240,232,0.82) 88%,
          rgba(245,240,232,1) 100%
        );
    }

    .hero-content {
      position: relative;
      z-index: 2;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 28px;
      padding: 0 24px;
      opacity: 0;
      transform: translateY(24px);
      animation: fadeUp 0.9s 0.2s ease forwards;
    }

    @keyframes fadeUp {
      to { opacity: 1; transform: translateY(0); }
    }

    .hero-wordmark {
      display: flex;
      flex-direction: column;
      align-items: center;
      line-height: 0.88;
    }

    .hero-wordmark .line1 {
      font-family: 'DM Sans', sans-serif;
      font-size: clamp(16px, 2.2vw, 26px);
      font-weight: 300;
      letter-spacing: 0.5em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.85);
      text-shadow: 0 2px 12px rgba(0,0,0,0.4);
    }

    .hero-wordmark .line2 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(90px, 14vw, 182px);
      letter-spacing: 0.02em;
      color: #fff;
      text-shadow: 0 4px 40px rgba(0,0,0,0.35), 0 0 80px rgba(42,122,75,0.2);
      line-height: 1;
    }

    .hero-tagline {
      font-family: 'DM Serif Display', serif;
      font-style: italic;
      font-size: clamp(15px, 1.8vw, 21px);
      color: rgba(255,255,255,0.75);
      text-shadow: 0 2px 10px rgba(0,0,0,0.3);
      letter-spacing: 0.01em;
      max-width: 400px;
    }

    .hero-search {
      display: flex;
      align-items: center;
      width: min(560px, 90vw);
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(255,255,255,0.6);
      border-radius: 6px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.18);
      overflow: hidden;
      transition: box-shadow 0.2s;
    }
    .hero-search:focus-within {
      box-shadow: 0 8px 32px rgba(0,0,0,0.22), 0 0 0 3px rgba(42,122,75,0.2);
    }

    .hero-search svg {
      flex-shrink: 0;
      margin-left: 18px;
      color: var(--muted2);
    }

    .hero-search input {
      flex: 1;
      background: none;
      border: none;
      outline: none;
      padding: 16px 14px;
      color: var(--text);
      font-family: 'DM Sans', sans-serif;
      font-size: 15px;
      font-weight: 300;
    }
    .hero-search input::placeholder { color: var(--muted2); }

    .hero-search button {
      background: var(--green);
      border: none;
      color: #fff;
      font-family: 'DM Sans', sans-serif;
      font-size: 13px;
      font-weight: 500;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      padding: 0 24px;
      height: 52px;
      cursor: pointer;
      transition: background 0.2s;
      white-space: nowrap;
    }
    .hero-search button:hover { background: var(--green-light); }

    .hero-scroll {
      position: absolute;
      bottom: 36px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 2;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 8px;
      opacity: 0;
      animation: fadeUp 0.8s 1.3s ease forwards;
    }
    .hero-scroll span {
      font-size: 10px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--muted);
    }
    .scroll-line {
      width: 1px;
      height: 36px;
      background: linear-gradient(to bottom, var(--muted), transparent);
      animation: scrollPulse 2s 1.6s ease-in-out infinite;
    }
    @keyframes scrollPulse {
      0%,100% { opacity: 0.5; }
      50% { opacity: 1; }
    }

    /* STATS */
    .stats-band {
      background: var(--sand);
      border-top: 1px solid var(--border2);
      border-bottom: 1px solid var(--border2);
    }

    .stats-inner {
      max-width: 1100px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
    }

    .stat-item {
      padding: 44px 40px;
      text-align: center;
      position: relative;
      opacity: 0;
      transform: translateY(14px);
      transition: opacity 0.6s, transform 0.6s;
    }
    .stat-item.visible { opacity: 1; transform: none; }
    .stat-item:nth-child(2) { transition-delay: 0.1s; }
    .stat-item:nth-child(3) { transition-delay: 0.2s; }
    .stat-item + .stat-item::before {
      content: '';
      position: absolute;
      left: 0; top: 25%; bottom: 25%;
      width: 1px;
      background: var(--border2);
    }

    .stat-number {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(42px, 5vw, 64px);
      color: var(--green);
      letter-spacing: 0.02em;
      line-height: 1;
    }
    .stat-label {
      font-size: 11px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--muted);
      margin-top: 8px;
    }

    /* MISSION */
    .mission {
      padding: 110px 48px;
      max-width: 860px;
      margin: 0 auto;
      text-align: center;
    }

    .mission-eyebrow {
      font-size: 11px;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: var(--green);
      margin-bottom: 22px;
      opacity: 0; transform: translateY(10px);
      transition: opacity 0.6s, transform 0.6s;
    }
    .mission-eyebrow.visible { opacity: 1; transform: none; }

    .mission-headline {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(30px, 5vw, 54px);
      line-height: 1.2;
      color: var(--text);
      opacity: 0; transform: translateY(14px);
      transition: opacity 0.7s 0.1s, transform 0.7s 0.1s;
    }
    .mission-headline.visible { opacity: 1; transform: none; }
    .mission-headline em { color: var(--green); font-style: italic; }

    .mission-body {
      margin-top: 26px;
      font-size: 17px;
      line-height: 1.85;
      color: var(--muted);
      max-width: 580px;
      margin-left: auto; margin-right: auto;
      opacity: 0; transform: translateY(10px);
      transition: opacity 0.7s 0.2s, transform 0.7s 0.2s;
    }
    .mission-body.visible { opacity: 1; transform: none; }

    /* PHOTO DIVIDER */
    .photo-divider {
      position: relative;
      height: 360px;
      overflow: hidden;
      margin: 0 48px;
      border-radius: 10px;
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.8s, transform 0.8s;
    }
    .photo-divider.visible { opacity: 1; transform: none; }

    .photo-divider-img {
      position: absolute;
      inset: 0;
      background-image: // insert image here
      background-size: cover;
      background-position: center 38%;
      filter: brightness(0.6) saturate(0.75);
    }

    .photo-divider-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        135deg,
        rgba(42,122,75,0.55) 0%,
        rgba(30,20,10,0.4) 100%
      );
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .photo-divider-text {
      font-family: 'DM Serif Display', serif;
      font-style: italic;
      font-size: clamp(24px, 4vw, 48px);
      color: rgba(255,255,255,0.92);
      text-align: center;
      max-width: 680px;
      padding: 0 32px;
      line-height: 1.3;
      text-shadow: 0 2px 20px rgba(0,0,0,0.3);
    }

    /* FEATURE CARDS */
    .features {
      padding: 80px 48px 100px;
      max-width: 1100px;
      margin: 0 auto;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 16px;
    }

    .feature-card {
      background: var(--warm-white);
      border: 1px solid var(--border2);
      border-radius: 8px;
      padding: 48px 44px;
      position: relative;
      overflow: hidden;
      transition: box-shadow 0.3s, transform 0.3s, background 0.2s;
      opacity: 0; transform: translateY(20px);
    }
    .feature-card.visible { opacity: 1; transform: none; }
    .feature-card:nth-child(2) { transition-delay: 0.1s; }
    .feature-card:nth-child(3) { transition-delay: 0.08s; }
    .feature-card:nth-child(4) { transition-delay: 0.18s; }

    .feature-card:hover {
      box-shadow: 0 8px 32px rgba(60,45,20,0.1);
      transform: translateY(-3px);
      background: #fff;
    }

    .feature-card::after {
      content: '';
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 3px;
      background: var(--green);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.4s ease;
    }
    .feature-card:hover::after { transform: scaleX(1); }

    .feature-icon {
      width: 44px; height: 44px;
      border-radius: 8px;
      background: var(--green-pale);
      border: 1px solid rgba(42,122,75,0.18);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 22px;
      color: var(--green);
    }

    .feature-title {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--muted2);
      margin-bottom: 10px;
    }

    .feature-headline {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(20px, 2.2vw, 28px);
      color: var(--text);
      line-height: 1.25;
      margin-bottom: 14px;
    }

    .feature-desc {
      font-size: 15px;
      line-height: 1.78;
      color: var(--muted);
    }

    /* CTA */
    .cta-section {
      margin: 0 48px 100px;
      background: var(--green);
      border-radius: 10px;
      padding: 72px 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 48px;
      position: relative;
      overflow: hidden;
      opacity: 0; transform: translateY(20px);
      transition: opacity 0.7s, transform 0.7s;
    }
    .cta-section.visible { opacity: 1; transform: none; }

    .cta-section::before {
      content: '';
      position: absolute;
      right: -60px; top: -60px;
      width: 320px; height: 320px;
      border-radius: 50%;
      background: rgba(255,255,255,0.07);
      pointer-events: none;
    }
    .cta-section::after {
      content: '';
      position: absolute;
      left: -40px; bottom: -80px;
      width: 260px; height: 260px;
      border-radius: 50%;
      background: rgba(0,0,0,0.06);
      pointer-events: none;
    }

    .cta-text h2 {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(26px, 3.5vw, 42px);
      color: #fff;
      line-height: 1.2;
      margin-bottom: 12px;
    }
    .cta-text p {
      font-size: 16px;
      color: rgba(255,255,255,0.75);
      line-height: 1.65;
      max-width: 420px;
    }

    .cta-actions {
      display: flex;
      gap: 12px;
      flex-shrink: 0;
      position: relative;
      z-index: 1;
    }

    .btn-primary {
      background: #fff;
      color: var(--green);
      border: none;
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      font-weight: 500;
      padding: 14px 28px;
      border-radius: 4px;
      cursor: pointer;
      transition: transform 0.15s, box-shadow 0.2s;
      white-space: nowrap;
    }
    .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(0,0,0,0.15); }

    .btn-secondary {
      background: none;
      color: rgba(255,255,255,0.88);
      border: 1px solid rgba(255,255,255,0.4);
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      font-weight: 400;
      padding: 14px 28px;
      border-radius: 4px;
      cursor: pointer;
      transition: border-color 0.2s, background 0.2s;
      white-space: nowrap;
    }
    .btn-secondary:hover { border-color: rgba(255,255,255,0.8); background: rgba(255,255,255,0.1); }

    /* FOOTER */
    footer {
      border-top: 1px solid var(--border2);
      padding: 32px 52px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: var(--sand);
    }
    .footer-logo {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 18px;
      letter-spacing: 0.1em;
      color: var(--muted);
    }
    .footer-logo span { color: var(--green); }
    .footer-copy {
      font-size: 12px;
      color: var(--muted2);
      letter-spacing: 0.04em;
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      nav { padding: 0 24px; }
      .stats-inner { grid-template-columns: 1fr; }
      .stat-item + .stat-item::before {
        top: 0; bottom: auto; left: 25%; right: 25%;
        width: auto; height: 1px;
      }
      .features { padding: 60px 20px 80px; }
      .features-grid { grid-template-columns: 1fr; gap: 12px; }
      .photo-divider { margin: 0 20px; height: 260px; }
      .cta-section {
        flex-direction: column; padding: 48px 28px;
        margin: 0 20px 60px; text-align: center;
      }
      .cta-text p { max-width: none; }
      .cta-actions { width: 100%; flex-direction: column; }
      .btn-primary, .btn-secondary { width: 100%; text-align: center; }
      footer { flex-direction: column; gap: 12px; text-align: center; padding: 28px 24px; }
      .mission { padding: 80px 24px; }
    }
  </style>
</head>
<body>

  <nav>
    <div class="nav-logo">Manager<span>Check</span></div>
    <div class="nav-links">
      <button class="nav-login">Login</button>
      <button class="nav-signup">Sign Up</button>
    </div>
  </nav>

  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <div class="hero-wordmark">
        <span class="line1">Manager</span>
        <span class="line2">CHECK</span>
      </div>
      <p class="hero-tagline">Know who you're working for, before you start.</p>
      <div class="hero-search">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
        </svg>
        <input type="text" placeholder="Search a restaurant…" />
        <button>Search</button>
      </div>
    </div>
    <div class="hero-scroll">
      <span>Scroll</span>
      <div class="scroll-line"></div>
    </div>
  </section>

  <section class="stats-band">
    <div class="stats-inner">
      <div class="stat-item">
        <div class="stat-number">14,200+</div>
        <div class="stat-label">Reviews submitted</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">100%</div>
        <div class="stat-label">Anonymous</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">3,800+</div>
        <div class="stat-label">Managers rated</div>
      </div>
    </div>
  </section>

  <section class="mission">
    <div class="mission-eyebrow">Our Purpose</div>
    <h2 class="mission-headline">
      Restaurant workers deserve to know <em>who</em> they're working for.
    </h2>
    <p class="mission-body">
      The industry moves fast and word doesn't always travel far enough. Manager Check gives hospitality workers a trusted, anonymous space to share their experiences — so the next person can make a more informed choice.
    </p>
  </section>

  <div class="photo-divider">
    <div class="photo-divider-img"></div>
    <div class="photo-divider-overlay">
      <div class="photo-divider-text">"The kitchen you see isn't always the kitchen you work in."</div>
    </div>
  </div>

  <section class="features">
    <div class="features-grid">

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>
          </svg>
        </div>
        <div class="feature-title">Community</div>
        <div class="feature-headline">Reviews from real workers, verified by experience</div>
        <div class="feature-desc">Every rating comes from someone who's actually worked a shift. No fake accounts, no management interference. Just honest feedback from people who've been in your shoes.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
            <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
          </svg>
        </div>
        <div class="feature-title">Privacy</div>
        <div class="feature-headline">All reviews are completely anonymous</div>
        <div class="feature-desc">Your name never appears. Your identity is never stored with your review. Speak freely — we've built the system specifically so you never have to worry about blowback.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
          </svg>
        </div>
        <div class="feature-title">Insight</div>
        <div class="feature-headline">Detailed ratings across what actually matters</div>
        <div class="feature-desc">We score managers on communication, fairness, approachability, and organization — the four dimensions that make or break a shift. Not just a single vague star rating.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <line x1="12" y1="8" x2="12" y2="12"/>
            <line x1="12" y1="16" x2="12.01" y2="16"/>
          </svg>
        </div>
        <div class="feature-title">Transparency</div>
        <div class="feature-headline">Find the best workplace before accepting the job</div>
        <div class="feature-desc">Browse any restaurant, see all its managers, and read what your future coworkers already know. Walk into your next job with your eyes open, not crossed fingers.</div>
      </div>

    </div>
  </section>

  <div class="cta-section">
    <div class="cta-text">
      <h2>Know before you go.</h2>
      <p>Join thousands of hospitality workers who use Manager Check to research, review, and protect themselves in the industry.</p>
    </div>
    <div class="cta-actions">
      <button class="btn-primary">Create Free Account</button>
      <button class="btn-secondary">Browse Restaurants</button>
    </div>
  </div>

  <footer>
    <div class="footer-logo">Manager<span>Check</span></div>
    <div class="footer-copy">© 2025 ManagerCheck · All reviews are anonymous.</div>
  </footer>

  <script>
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('visible');
      });
    }, { threshold: 0.1 });

    document.querySelectorAll(
      '.stat-item, .feature-card, .mission-eyebrow, .mission-headline, .mission-body, .cta-section, .photo-divider'
    ).forEach(el => observer.observe(el));
  </script>
</body>
</html>